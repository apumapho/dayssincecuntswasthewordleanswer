{
  "name": "days-since-cunts-was-the-wordle-answer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "update": "node scripts/update-data.mjs",
    "import-seed": "node scripts/import-seed.mjs",
    "dns:check": "node scripts/configure-porkbun-dns.mjs",
    "dns:apply": "node scripts/configure-porkbun-dns.mjs --apply",
    "schedule:install": "zsh scripts/install-launch-agent.sh",
    "test": "node --test",
    "serve": "npx --yes serve public"
  },
  "engines": {
    "node": ">=20"
  }
}
